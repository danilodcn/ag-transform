version: '3.4'

services:
  app:
    image: app_tcc
    container_name: app_tcc
    command: 'tail -f /dev/null'
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      -  ENVIROMENT:develop
    volumes:
      - ".:/home/python/app/"
